<Window x:Class="ChatTool.Views.Main.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatTool.Views.Main"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">

    <Grid x:Name="grid" MinWidth="300" MinHeight="438">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3.5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid Grid.RowSpan="2" x:Name="ChannelListViewAria" >
            <local:ChannelListView />
        </Grid>

        <Grid Grid.Row="1" Grid.Column="1" x:Name="InputAreaViewAria" >
            <local:InputAreaView />
        </Grid>

        <Grid Grid.Row="0"  Grid.Column="1" x:Name="MessageLogViewAria">
            <local:MessageLogView />
        </Grid>

    </Grid>
</Window>
